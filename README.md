# 🚀 DevSpace | Developer Profile Analytics Engine

DevSpace is a high-performance web application designed to optimize talent evaluation and profile discovery. By consuming the public GitHub REST API, the platform aggregates user data across up to 100 repositories simultaneously, executing client-side metrics parsing to deliver dynamic linguistic charts and curated repository performance tracking.

## ✨ Key Value Propositions
* **Dynamic Language Aggregation:** Automatically processes linguistic footprints across deep repository histories, mapping out a visual percentage-based chart.
* **Impact-Driven Curation:** Automatically prioritizes and highlights featured repositories based on engagement metrics (Stars & Forks), bypassing default alphabetical sorting.
* **Enterprise UI/UX:** Built with a clean, high-contrast dashboard interface optimized for fast and non-technical technical recruitment evaluation workflows.

## 🛠️ Built With
* **Framework:** React 18 (Vite)
* **Data Visualization:** Recharts (SVG-driven responsive layout engine)
* **Icons:** Lucide React
* **Styling:** Custom High-Contrast Architectural Component CSS

## ⚡ Technical Challenges Solved
1. **Decoupled Architecture:** Kept data aggregation functions completely separated from rendering components to optimize state changes.
2. **Layout Fallback Resilience:** Engineered with a specialized inline-object structural style architecture to ensure visual layout integrity across varying deployment build environments.
3. **Asynchronous Exception Handling:** Implemented defensive code patterns to gracefully capture API rate boundaries, null values, and 404 user states without breaking the active UI cycle.

## 📦 Local Deployment Instructions

To get a local copy up and running, follow these simple steps:

1. Clone the repository:
   git clone [https://github.com/maniteja0407/devspace.git](https://github.com/maniteja0407/devspace.git)

2. Navigate into the project directory:
cd devspace

3. Install dependencies:
npm install

4. Start the local development server:
npm run dev

---

### Step 3: Push the updates to GitHub
Once you have saved the `README.md` file in VS Code, run these three quick commands in your terminal to update your repository online:

```bash
git add README.md
git commit -m "docs: upgrade README with comprehensive architecture breakdown"
git push
